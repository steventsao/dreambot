import React from 'react';

const Login = React.createClass({
  render() {
    return (
      <div>
        <button>
        Login with Github
        </button>
      </div>
    )
  }
})

export default Login;