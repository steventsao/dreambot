jest
  .unmock('../app/actions/index');

// var getMessageVolume = require.requireActual('../app/actions/index').getMessageVolume;
import { getMessageVolume } from '../app/actions/index';

import * as queries from './__mocks__/queries'
describe('User engagement', () => {
    // it('should return channel engagement', () => {

    // })
})
